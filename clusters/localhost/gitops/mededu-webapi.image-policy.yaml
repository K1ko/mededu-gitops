apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: mededu-webapi
  namespace: wac-hospital
spec:
  imageRepositoryRef:
    name: mededu-webapi
  filterTags:
    pattern: "main.*"
  policy:
    alphabetical:
      order: asc
