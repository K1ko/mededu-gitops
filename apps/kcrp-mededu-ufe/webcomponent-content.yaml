apiVersion: polyfea.github.io/v1alpha1
kind: WebComponent
metadata:
  name: kcrp-mededu-trainings-content
spec:
  microFrontend: kcrp-mededu-ufe
  element: kcrp-mededu-trainings-app
  attributes:
    - name: base-path
      value: /kcrp-mededu-trainings/
    - name: api-base
      value: /kcrp-api
  displayRules:
    - allOf:
      - context-name: main-content
      - path: "^(\\.?/)?kcrp-mededu-trainings(/.*)?$"
