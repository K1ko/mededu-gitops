apiVersion: apps/v1
kind: Deployment
metadata:
  name: kcrp-mededu-webapi
spec:
  template:
    spec:
      containers:
        - name: openapi-ui
          env:
            - name: URL
              value: /kcrp-openapi
            - name: BASE_URL
              value: /kcrp-openapi-ui
